---
title: Reproductibilitat en bioinformàtica
subtitle: "Del caos al control: com assegurar la traçabilitat en les anàlisis"
format:
  physmed-portrait-typst: 
    poster-authors: "Max Gubert$^{1}$, Ignasi del Salto$^{2}$,Pep Mut$^{2}$, Maria Berman-Riu$^{1}$, Josep Muncunill$^{1}$"
    poster-banner: "images/banner-poster-physmed-logos-portrait.png"
    departments: "$^1$Plataforma de Genòmica i Bioinformàtica \n\n\n\n
    $^2$PRISIB"
    footer-text: "Plataforma Genòmica i Bioinformàtica"
    footer-url: "https://www.idisba.es"
    footer-emails: "idisba.bioinformatica@idisba.es"
    footer-color: "C0D8D4"
    space-after-header: 60
    margin-top: 0.5
    num-columns: 2
    

   
editor_options: 
  chunk_output_type: console
---

# Introducció

La **reproductibilitat** és el grau de concordança entre mesuraments independents realitzats amb el mateix mètode i material, però en condicions diferents.

En **bioinformàtica**, la reproductibilitat és pot entendre com la capacitat d’utilitzar els materials d’un estudi anterior (com ara dades, codi i documentació) per regenerar els resultats, incloent figures i taules, amb l’objectiu de confirmar les conclusions de l’estudi.

Garantir la reproductibilitat és essencial en un camp on el volum de dades és immens, el programari evoluciona ràpidament i la diversitat d’entorns de programació és elevada. Sense un control rigorós de tots aquests punts, petites variacions poden alterar les conclusions i comprometre seriosament la recerca.

```{r,eval=F,echo=F}
library(DiagrammeR)
library(DiagrammeRsvg)
library(rsvg)

g <- grViz("
digraph cdoi {
  graph [rankdir=LR, bgcolor=\"white\", nodesep=0.8, ranksep=1.2]

  node [fontname=\"Helvetica\", fontsize=18, shape=box, style=filled, penwidth=1.5]

  # Nodes principals
  Control  [label=<
    <b>☝Control</b><br/><font point-size='16'>Gestió i traçabilitat</font>
  >,
  shape=rectangle, fillcolor=\"#E0F2FE\", color=\"#0284C7\", width=1.6]

  DataIN [label=<
    <b>📂Data in</b><br/><font point-size='16'>Dades originals</font>
  >,
  fillcolor=\"#DCFCE7\", color=\"#16A34A\"]

  Codi [label=<
    <b>💻 Codi</b><br/><font point-size='16'>Scripts, pipelines</font>
  >,
  fillcolor=\"#F3E8FF\", color=\"#9333EA\"]

  Entorn [label=<
    <b>⚙️ Entorn</b><br/><font point-size='16'>Versió i dependències</font>
  >,
  fillcolor=\"#FEF9C3\", color=\"#CA8A04\"]

  DataOUT [label=<
    <b>📊 Data out</b><br/><font point-size='16'>Resultats i informes</font>
  >,
  fillcolor=\"#FFE4E6\", color=\"#BE123C\"]

  # Nodes secundaris (detalls)
  DataIN_det [label=<
    <font point-size='16'>➕➖ Addició o eliminació<br/>de pacients, variables</font>
  >, shape=note, fillcolor=\"#ECFDF5\", color=\"#16A34A\", fontsize=14]

  Codi_det [label=<
    <font point-size='16'>🧩 Modificació d’scripts<br/>o noves funcions</font>
  >, shape=note, fillcolor=\"#F5F3FF\", color=\"#9333EA\", fontsize=14]

  Entorn_det [label=<
    <font point-size='16'>🔄 Actualització de<br/>llibreries o software</font>
  >, shape=note, fillcolor=\"#FEFCE8\", color=\"#CA8A04\", fontsize=14]

  DataOUT_det [label=<
    <font point-size='16'>⚠️ Lectura o exportació<br/>amb configuracions errònies</font>
  >, shape=note, fillcolor=\"#FFF1F2\", color=\"#BE123C\", fontsize=14]

  # Connexions principals
  edge [color=\"#64748B\", arrowsize=1.1, penwidth=1.5]
  Control -> DataIN
  Control -> Codi
  Control -> Entorn
  Control -> DataOUT

  # Connexions secundàries
  edge [style=dashed, color=\"#94A3B8\", arrowsize=0.9]
  DataIN -> DataIN_det
  Codi -> Codi_det
  Entorn -> Entorn_det
  DataOUT -> DataOUT_det
}
")


svg_txt <- export_svg(g)
cat(svg_txt, file = "./images/cdoi_flowchart.svg")
rsvg_pdf("./images/cdoi_flowchart.svg", "./images/cdoi_flowchart.pdf")
rsvg_png("./images/cdoi_flowchart.svg", "./images/cdoi_flowchart.png", width = 2200, height = 1200)
```

![](/images/cdoi_flowchart.png){width="100%"}

# Metodologia

S’ha implementat una bateria de funcions i eines —tant pròpies com de tercers— per garantir la reproductibilitat en totes les fases del procés analític.

```{typst,echo=F}

#grid(
columns: (1fr, 1fr),
column-gutter: 0.9in,
align: top,

[
#heading(level: 2)[Control de dades]
#text(22pt)[Afegim metadades (creació, modificació, últim accés) i suma MD5 per garantir integritat.]
#v(8pt)
#image("images/control_dades_timeline.png", width: 100%)
],

[
#heading(level: 2)[Control de codi]
#text(22pt)[Git per a duplicació, treball en paral·lel i fusió: traçabilitat del codi i autoria.]
#v(8pt)
#image("images/control_codi_git.png", width: 100%)
]
)

```

## Control Dades d'Entrada

```{r,echo=F}

read_controlled <- function(path) {
  data <- read.csv(path)
  attr(data, "created_at") <- file.info(path)$ctime
  attr(data, "modify_at") <- file.info(path)$mtime
  attr(data, "last_acces_at") <- file.info(path)$atime
  attr(data, "md5") <- tools::md5sum(path)
  data
}
```

```{r,echo=F}
path<-"/home/josep/20240601_amanda_bioRNA_biagra_RESCAT2/RESULTATS_BIOPSIA_RNA_nou/TAULES/table_DEG_sig_RNA_score.csv"
dades <- read_controlled(path)

```

Per al control de les dades d’entrada, s’afegeix una capa de metadades amb informació sobre la **data de creació, modificació i últim accés**, així com l'**MD5sum**.\
Amb aquesta informació es pot identificar qualsevol canvi en els arxius inicials, garantint la traçabilitat.

**Detall de metadades afegides:**

-   Data de creació, modificació i últim accés

-   MD5sum: identificador que permet detectar qualsevol modificació, per petita que sigui, en un fitxer

```{r,results='markup',echo=F,include=F}
info<-attributes(dades)

print(info$created_at)

print(info$last_acces_at)

print(info$modify_at)

```

```{r,echo=F,warning=F,message=F,fig.width=20, fig.height=7, dpi=900}
library(dplyr)
library(ggplot2)
library(lubridate)
library(tibble)

# --- dades (ja les tens) ---
temps <- tibble(
  tipus  = c("Creació","Modificació","Eliminar\nregistre","Últim\n accés"),
  moment = as.POSIXct(c(info$created_at, info$modify_at,info$modify_at, info$last_acces_at),
                      origin = "1970-01-01"),
  icona  = c("📄","✍","✏️❌	","🔎️")
) |>
  mutate(moment = if_else(tipus == "Creació", moment - days(20), moment))|>
  mutate(moment = if_else(tipus == "Eliminar\nregistre", moment + days(40), moment))|>
  arrange(moment)

# --- paràmetres d'alçada ---
y_baseline <- 0         # línia i punts
y_icons    <- 0.29      # icones a dalt
y_label    <- -0.27     # text "Creació/Modificació/Últim accés"
y_date     <- -0.60     # data

# --- ploteig amb capes a y diferents i sense clip ---
p<-
ggplot() +
  # línia temporal
  geom_line(data = temps, aes(x = moment, y = y_baseline),
            linewidth = 2, color = "grey60") +
  # punts
  geom_point(data = temps, aes(x = moment, y = y_baseline),
             size = 9, shape = 21, fill = "white", color = "black") +
  # icones
  geom_text(data = temps, aes(x = moment, y = y_icons, label = icona),
            size = 29) +
  # etiquetes
  geom_text(data = temps, aes(x = moment, y = y_label, label = tipus),
            size = 15) +
  # dates (sense hores)
  geom_text(data = temps, aes(x = moment, y = y_date,
                              label = format(moment, "%Y-%m-%d")),
            size = 10, color = "grey30") +
  scale_x_datetime(date_labels = "%Y-%m-%d",
                   expand = expansion(mult = c(0.05, 0.05))) +
  scale_y_continuous(limits = c(-0.6, 0.6)) +
  coord_cartesian(clip = "off") +  # evita que talli el text/emoji
  theme_void(base_size = 35) +
  theme(plot.margin = margin(20, 20, 20, 20) # dona aire a dreta/esq/baix
  )

p
```

## Control codi

Per al control del codi s’utilitza un gestor **Git**, que permet fer seguiment dels canvis, guardant les diferents versions i recuperant qualsevol versió anterior.\

En el cas d'un codi compartit entre diferents persones, Git identifica qui ha fet cada modificació i facilita la fusió de versions paral·leles, assegurant la coherència i la traçabilitat en entorns col·laboratius.

```{r,echo=F,warning=F,message=F,fig.width=17, fig.height=8, dpi=900}
library(ggplot2)
library(dplyr)
library(ggrepel)

# --- Dades que defineixen el flux ---
# x = temps, y = "nivell" (0=fitxer principal, 1=usuari1, -1=usuari2)
versions <- tibble::tribble(
  ~id, ~x, ~y, ~fase,        ~autor,    ~label,     ~emoji,
  1,   1,  0,  "Original",   "",   "", "📒",
  2,   2,  1,  "Còpia",      "",  "", "📄",
  3,   2, -1,  "Còpia",      "",  "", "📄",
  4,   3,  1,  "Treball",    " ",  "", "📝",
  5,   3, -1,  "Treball",    "",  "", "📝",
  6,   4,  0,  "Fusió",      "",   "", "📚"
)

# --- Arestes (connexions entre punts) ---
connexions <- tibble::tribble(
  ~x1, ~y1, ~x2, ~y2,
  1, 0, 2, 1,     # Branch cap a Usuari 1
  1, 0, 2, -1,    # Branch cap a Usuari 2
  2, 1, 3, 1,     # Treball Usuari 1
  2, -1, 3, -1,   # Treball Usuari 2
  3, 1, 4, 0,     # Merge cap a principal
  3, -1, 4, 0     # Merge cap a principal
)
size_text<-15
# --- Gràfic ---
p <- ggplot() +
  geom_curve(
    data = connexions,
    aes(x = x1, y = y1, xend = x2, yend = y2),
    curvature = 0.05,
    linewidth = 2.2,
    color = "grey40",
    arrow = arrow(length = unit(9, "pt"), type = "closed")
  ) +
  geom_point(
    data = versions,
    aes(x = x, y = y),
    size = 14, shape = 21, fill = "white", color = "black", stroke = 1.4
  ) +
  geom_text(
    data = versions,
    aes(x = x, y = y, label = emoji),
    size = size_text, vjust = -1.5
  ) +
  geom_text(
    data = versions,
    aes(x = x, y = y, label = autor),
    vjust = 1.8, size = size_text, family = "sans"
  ) +

  scale_y_continuous(
    limits = c(-2.2, 2.2),
    breaks = c(-1, 0, 1),
    labels = c("👤  Persona 2", "", "👤 Persona 1")
  ) +
  scale_x_continuous(
    breaks = 1:4,
    labels = c("Versió inicial", "Duplicació", "Edició", "Última versió")
  ) +
  theme_minimal(base_size = 22) +
  theme(
    panel.grid.minor = element_blank(),
    panel.grid.major = element_blank(),
    axis.title = element_blank(),
    axis.text = element_text(color = "grey20", size = 35),
    plot.title = element_text(size = 35, face = "bold", hjust = 0.5),
    plot.margin = margin(40, 60, 40, 60)
  )

p


```

## Control de l'entorn

Els canvis i/o actualitzacions en el software poden implicar canvis en algoritmes que impedeixen la reproductibilitat dels resulats.

Per evitar-ho, s’han implementat diferents eines que permeten capturar i fixar l’estat de l’entorn: **Docker**, **renv** i **sessionInfo()**

```{r,echo=F,warning=F,message=F,fig.width=16, fig.height=10, dpi=900}
# Escala de control d'entorn: de simple a avançat
library(ggplot2)
library(dplyr)
library(tibble)

nivells <- tribble(
  ~ordre, ~etiqueta,                                       ~desc,                                                                 ~icona,
  1,      "Versions\nsoftware",                    "Informació sessió",                          "🛴️",
  2,      "renv",                   "Aïllament de paquets",           "🚗",
  3,      "Docker",    "Entorn complet\nportable",               "🚀"
)

# dibuix tipus 'esglaons': segment 0→ordre i punt final amb icona/etiqueta
ggplot(nivells, aes(x = ordre, y = reorder(etiqueta, ordre))) +
  # segments d'esglaó
  geom_segment(aes(x = 0.7, xend = ordre, yend = reorder(etiqueta, ordre)),
               linewidth = 6, lineend = "round", color = "grey75") +
  # punt final de cada nivell
  geom_point(size = 10, shape = 21, fill = "white", color = "black") +
  # icona damunt del punt
  geom_text(aes(label = icona), vjust = -1, size = 14) +
  # descripció sota
  geom_text(aes(label = desc), vjust = c(2,2,1.5), size = 10, color = "grey25") +
  # anotacions d'eix
  scale_x_continuous(limits = c(0.7, 3.3), breaks = 1:3,
                     labels = c("Bàsic", "Mitjà", "Avançat")) +
  labs(
    title = "",
    x = "", y = NULL
  ) +
  
  theme(
    panel.grid.major.y = element_blank(),
    panel.grid.minor = element_blank(),
    axis.title.y = element_blank(),
    plot.title = element_text(face = "bold", hjust = 0.5),
    plot.margin = margin(20, 40, 20, 20)
  )+theme_minimal(base_size = 45)
```

# Conclusions

-   La **Plataforma de Genòmica i Bioinformàtica** posa a disposició dels usuaris un servidor amb les eines necessàries per assolir la reproductibilitat.

-   Des de la plataforma oferim **formació** específica en aquest àmbit.\

::: {.callout-note icon="false" title="🖀"}

Aquest póster s'ha generat integrament amb Quarto, des del text fins al codi QR. Si vols aprendre com, posa't en contacte amb nosaltres.

![](images/qrcode-poster.svg){fig-align="center" width="270"}
:::
